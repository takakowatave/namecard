specVersion: "v1beta"
serviceId: "namecard"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "namecard-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
